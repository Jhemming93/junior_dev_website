---
deployment:
      tasks:
        - export DEPLOYPATH=../../public_html/junior_dev_website/
        - cp -r app $DEPLOYPATH
        - cp -r public $DEPLOYPATH
        - cp -r node_modules $DEPLOYPATH
        - cp -r builds $DEPLOYPATH
        - cp -r composer.json $DEPLOYPATH
        - cp -r composer.lock $DEPLOYPATH
        - cp -r package.json $DEPLOYPATH
        - cp -r package-lock.json $DEPLOYPATH
        - cp -r preload.php $DEPLOYPATH
        - cp -r spark $DEPLOYPATH 
        - cp -r phpunit.xml.dist $DEPLOYPATH  
        # need to change the upload paths and how things are aranged for stuff on site and stuff other people upload