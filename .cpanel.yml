---
deployment:
      tasks:
        - export DEPLOYPATH=../../public_html/junior_dev_website/
        - cp -r application $DEPLOYPATH
        - cp -r assets $DEPLOYPATH
        - cp -r css $DEPLOYPATH
        - cp -r img $DEPLOYPATH
        - cp -r js $DEPLOYPATH
        - cp -r system $DEPLOYPATH
        - cp -r trumbowyg $DEPLOYPATH
        - cp .htaccess $DEPLOYPATH
        - cp index.php $DEPLOYPATH
        # need to change the upload paths and how things are aranged for stuff on site and stuff other people upload